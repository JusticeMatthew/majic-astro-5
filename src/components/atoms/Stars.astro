<div id="fading-stars-container">
  <figure
    class="absolute inset-0 h-full w-screen bg-gradient-to-b from-dark-purple via-dark-bg via-80% to-dark-bg">
  </figure>
  <slot name="examples" />
</div>
<div id="normal-stars-container">
  <slot name="contact" />
  <slot name="footer" />
</div>

<style is:global>
  #fading-stars-container,
  #normal-stars-container {
    position: relative !important;
    width: 100%;
    height: fit-content !important;
    overflow: hidden;
    z-index: 10;
  }

  #star {
    position: absolute !important;
    width: 2px;
    height: 2px;
    background-color: #fff;
    border-radius: 50%;
    opacity: var(--dynamic-opacity);
    animation: twinkle 1s infinite;
  }

  @keyframes twinkle {
    0%,
    100% {
      opacity: var(--dynamic-opacity);
    }
    50% {
      opacity: 0.3;
    }
  }
</style>
